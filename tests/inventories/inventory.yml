all:
  children:
    master:
      hosts:
        master-node:
    worker:
      hosts:
        worker-1-node:
          roles: ['aws-cli', 'nodejs']
        worker-2-node:
          roles: ['aws-cli', 'dotnet', 'dotnet-ef', 'docker-cli']