all:
  children:
    master:
      hosts:
        master-node:
    worker:
      hosts:
        worker-1-node:
          roles: ['nodejs']
        worker-2-node:
          roles: ['dotnet', 'dotnet-ef', 'docker-cli']